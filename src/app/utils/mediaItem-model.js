export class MediaItem {
    constructor(id, photograperId, tags, likes, date, price) {
        id = id;
        photograperId = photograperId;
        tags = tags;
        likes = likes;
        date = date;
        price = price;
    }
}